{
  "spec": {
    "template": {
      "spec": {
        "containers": [
          {
            "name": "fastapi-data-loader",
            "imagePullPolicy": "Always"
          }
        ]
      }
    }
  }
}